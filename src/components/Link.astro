---
import { Icon } from "astro-icon/components";

interface Props {
  url: string;
  label?: string;
  skip?: boolean;
  icon?: string;
  className?: string;
  active?: boolean;
}

const { url, label, className, skip, icon, active } = Astro.props as Props;
const baseStyle =
  "flex gap-2 items-center justify-center transition hover:scale-105 hover:text-accent-hover";
const skipStyle =
  "absolute top-0 left-4 z-50 -translate-y-12 px-6 py-2 rounded-md font-bold text-text bg-accent-soft transition focus:translate-y-4";
const activeStyle = "font-bold text-accent border-b-2 border-accent";
---

<a
  href={url}
  class={`${skip ? skipStyle : baseStyle} ${className} ${active ? activeStyle : ""}`}
>
  {icon && <Icon name={icon} class="text-xl" />}
  {label}
</a>
